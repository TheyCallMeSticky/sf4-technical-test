{% extends 'base.html.twig' %}
{% block body %}

    Hello World!

    {% if user %}
        {{dump(user) }}
        <a href="{{ path('logout') }}" >Logout</a>
    {% else %}
        <a href="{{ path('connect_github') }}" >Login</a>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}


{% endblock %}